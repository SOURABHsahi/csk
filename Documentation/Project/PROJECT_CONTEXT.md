# PROJECT_CONTEXT

## Purpose

This document provides the current state of the project for future AI sessions.

It should always remain concise (1–2 pages).

Update this document after every completed implementation phase.

---

## Current Version

v1.2.8 (Phase E Completed — Backend Frozen for Frontend Handoff)

---

## Current Phase

Phase E — Release Activities & Documentation Sync (100% Completed — Backend Frozen)

---

## Project Status

### Completed Modules

- Phase 1 — Infrastructure & Core Architecture
- Phase 2 — Authentication Foundation
- Phase 3 — User Profile & Management
- Phase 4 — Content Foundation & Interaction Engine
- Phase 5 — Communities & Membership
- Phase 6 — Post & Article Engines
- Phase 7 — Media Channels (Video & Podcast)
- Phase 8 — Dashboard Feed & Gamification
- Phase 9 — Global Search & Discovery
- Phase 10 — Audit Trail & Governance (10.1 Infrastructure, 10.2 Suspension Guard, 10.3 Audit Read API, 10.4 Governance Wiring, 10.5 Guard Extension)
- Phase 11 — Jobs & Notifications (generic notification engine)
- Phase 12 — Notification Producers & Final Hardening (100% wired notifications, DI validated, and verified via end-to-end integration test suite)
- Defect Resolution Pass — Reconciled HTTPS redirection, AuthService suspension checks, global ForbiddenException mapper, and JobRepository error handling
- Production Hardening Pass — Enforced strict FK validity checks (`GBV-001`, `M-001/002`), global `ValidationFilter` for automatic DTO validation interception, comprehensive `MaximumLength` constraints across all 12 modules (`B4-001`), and exact status codes/endpoints (`B7-001/002`, `B6-003/B8-001`)
- Phase A (Pre-Frontend Essential Fixes) — Completed F-009 (CreateJob 201 Created), F-010/F-011 (Karma & Notifications role constants), F-012 (AuditLogFilterDto Range validation), F-014 (InteractionController route regex constraints), F-015 (Pagination added to GetMyPosts, GetMyArticles, GetMyVideos, GetMyPodcasts), F-016 (SearchController uniform route template), and F-007 (Scratch project DI registrations)
- Phase B (Architecture Consistency) — Completed F-013 (Unified `KnomeControllerBase` across all 14 controllers), F-028 (Removed direct `_db` access from `UserService` and `CommunityService`), F-029/F-030 (Replaced magic strings with `NotificationTypes.*` constants), F-031 (Renamed generic `_service` fields), and F-020 (Added `ILogger` exception logging in `DeleteProfileImageAsync`)
- Phase C (Performance & Hardening) — Completed F-017 (Bulk `AddRangeAsync` + single `SaveChangesAsync` for notification broadcasts), F-026 (Restricted `KarmaController.AwardKarma` to `Roles.SystemAdmin`), and verified F-027 (`ChangeRoleDto.RoleNames` pre-validation via `ChangeRoleValidator`)
- Phase D (Security Hardening) — Completed F-018 (Environment-specific JWT secret override), F-019 (MIME/magic byte validation in `SaveProfileImageAsync`), F-021 (Explicit `AllowedHosts` configuration), F-022 (`SecurityHeadersMiddleware` pipeline registration), F-023 (Login rate limiting via `LoginRateLimiter`), and F-024 (`PiiScrubbingEnricher` registration in Serilog)
- Phase E (Release Activities & Documentation Sync) — Completed F-008 (`Backend_Verification_Findings.md` synchronized to post-hardening baseline), F-032 (`Production_Readiness_Audit_Part1.md` AutoMapper profile count corrected to 10 distinct profiles), verified post-launch deferral of F-025 (Refresh token infrastructure decision), and declared **Backend Freeze (`v1.2.8`)**.
- Phase 26 (Admin Post Deletion & Cascade Hardening) — Extended admin role authorization in `PostService` to cover all administrators (System Admin, Community Admin, HR Admin) and role codes (`SYSADM`, `CADM`, `HRADM`). Atomically cascade child table deletions in `PostRepository` (`PostMentions`, `PostAudienceCommunities`, `PostAudienceUsers`, comments, reactions, bookmarks, moderation reports, notifications). Fixed author/admin permissions in `PostCard.jsx` and added instant local feed removal via `post-deleted` event dispatch.
- Phase 37 (Post Scheduling Pipeline & Feed Privacy Fix) — Fixed scheduled post visibility leak in `PostRepository.cs` feed query. Aligned validator messages in `CreatePostValidator` and `UpdatePostValidator`. Added author controls (Publish Now & Cancel to Drafts) in `PostCard.jsx` and dedicated "⏰ Scheduled" filter tab in `Posts.jsx`.
- Phase 38 (Community Module End-to-End Excellence & Safeguards) — Linked `AudienceCommunityIds` to `CommunityPosts` in `PostService.cs`. Expanded `communitiesApi` with `createPost`, `pinPost`, `addAdmin`, and `removeAdmin`. Enforced 3-pinned post limit (`FR-CM-06`), Default Org community mandatory membership lock (`FR-CM-04`), Sole Admin safeguard (`FR-CM-05`), and live backend file uploads via `mediaApi.uploadFile` in `CommunityView.jsx`.
- Phase 139 (Unique View Count Per User Enforcement Across All Content) — Created `ContentViews` table in SQL Server with composite unique constraint on `(ContentType, ContentId, UserId)` and added `ViewCount` to `Posts` table. Re-scaffolded EF Core models. Enforced strict 1-view-per-user tracking across Posts, Articles, Videos, and Podcasts with zero extra increments on multiple views by the same user. Live verification passed 100%.
- Phase 140 (Remove Views Count Display and Tracking From Posts) — Removed the view count badge and automated view-recording hooks from `PostCard.jsx` across all feeds (Home feed, Communities, User Profile, Search). Articles, Videos, and Podcasts retain their strict unique 1-view-per-user tracking. Verified with clean frontend build and IIS sync.
- Phase 141 (Platform-Wide Real-Time Search Term Word Highlighting) — Implemented universal `HighlightText` component across the entire platform. Real-time search word highlighting is active as users type across 18 distinct areas: Top Navbar search suggestions & notifications drawer, Universal Search results, Posts feed & topic tags, Articles, Videos, Podcasts, Communities catalog & member/files lists, Network directory, Saved bookmarks, modals (CreatePost audience, ArticleShare, ShareProfile, CreateCommunity, SuspendUser), Role FAQs & User Manuals, HR Analytics (communities breakdown & audit logs), and Admin Console (moderation reports, Serilog live technical/human stream, community approvals, role requests). Tested and mirrored to IIS.
- Phase 146 (Role FAQ Modal Single Manual Download & FAQ-Only Focus) — Removed multi-tab manual viewer and sub-tab selector from `RoleFaqModal.jsx`, focusing the modal exclusively on clear and concise FAQs, role rules, and guidelines. Removed duplicate download buttons, establishing a single prominent `Download User Manual` button in the header that invokes the high-fidelity A4 vector PDF print preview.
- Phase 147 (Knome Frequently Asked Questions Headline & Plain Language Upgrade) — Changed modal headline to `Knome Frequently Asked Questions`, simplified subtitle to plain English, and completely rewrote all 21 questions and answers across all four roles into clear, friendly, and easy-to-understand language.

- Phase 153 (Community Share With Users Full Employee Roster & Search Filter) — Activated live employee search and full roster retrieval in the Community "Share with Users" modal.
- Phase 154 (Community Member Permanent Removal & Persistence Fix) — Implemented `DELETE /api/communities/{communityId}/members/{targetUserId}` in `CommunityController` and `CommunityService`. Updated `CommunityRepository.GetMembersAsync` to filter approved members by default. Enhanced frontend `handleConfirmRemoveMember` to call backend delete, update `knome_community_removed_${communityId}` persistent tombstone, filter removed users on refresh, and clean up joined status. Tested and verified on live SQL Server.
- Phase 155 (Remove Add Members Button From Community Members Header) — Removed the redundant `+ Add Members` button from the "Members & Roles" tab header in `CommunityView.jsx`, preserving the `Search members...` filter field for searching community members.
- Phase 156 (Remove Archive and Code Script Options from Upload Community File Modal) — Removed Archive and Code Script from the category dropdown in `CommunityView.jsx` and `AdminConsole.jsx`.
- Phase 157 (Files & Media Audio, Video, and Document Buttons Visibility and Alignment) — Replaced single-row horizontal clipping and overflow scrollbar in Community "Files & Media" tab with responsive `flex flex-wrap items-center gap-2`. Explicitly added and aligned `All Files`, `Documents`, `Audio`, `Videos`, and `Images` buttons with icons and dynamic live count badges. Enhanced file cards with dedicated `audiotrack` icon, "Listen" inline action, Audio upload category dropdown option, and HTML5 audio player preview modal. Enforced flex-wrap on `SavedContent.jsx` tabs as well. Production verified and deployed to IIS.
- Phase 158 (Community Files & Media Audio, Video, and Document Viewers & Playback Fix) — Resolved root causes of broken URLs, blank screens, and inactive placeholders across Audio, Video, and Document files in Community "Files & Media". Placed self-contained sample media assets in `public/media/` (`sample-audio.mp3`, `sample-video.mp4`, `sample-doc.docx`) with resilient HTTPS fallbacks. Upgraded modal previewer with full HTML5 audio player (play/pause, volume, seek, animated equalizer waves, download track), full HTML5 video player (play/pause, volume, seek, fullscreen, download video), embedded PDF viewer, and structured Word/DOCX document reader card with direct file download actions. Fixed `handleFileUploadSubmit` and `safeSetStorage` type-aware fallbacks preventing audio/video files from becoming image URLs. Healed legacy seed files and cached items, and eliminated grey navigation tab strip scrollbar on Windows. Production verified and deployed to IIS.
- Phase 47 (Enterprise Post Scheduling Workflow & DD/MM/YYYY Refactor) — Overhauled `CreatePostModal.jsx` scheduling popover with Knome enterprise styling, Indian standard `DD/MM/YYYY, hh:mm A` format, minimum 1-minute scheduling with `+1 Min` preset, live countdown card, modal footer scheduled pill chip, and dynamic submit button. Fixed UTC-to-Local timezone comparison bug in `PostService.cs` and `PostRepository.cs` where unreleased scheduled posts were improperly publishing immediately. Verified via live E2E test on SQL Server.
- Phase 48 (Enterprise Article Scheduling Workflow & Activation) — Activated complete article scheduling pipeline across `CreateArticleModal.jsx`, `Articles.jsx` (full-page editor mode and article listing), `articleService.js`, and backend (`CreateArticleDto`, `UpdateArticleDto`, `ArticleDto`, `ArticleRepository`, `ArticleService`, and `ScheduledPostHostedService`). Implemented `DD/MM/YYYY, hh:mm A` standard date formatting, 1-minute scheduling, 6 quick presets (`+1 Min` to `Tomorrow 9 AM`), live preview card, active scheduled pill chip, author privacy enforcement (scheduled articles stay private until release), `"⏰ Scheduled"` queue tab, and immediate `"Publish Now"` override. Fully verified via live end-to-end integration test against SQL Server.
- Phase 49 (Article Modal Rich Text WYSIWYG Toolbar Activation) — Transformed static formatting toolbar in `CreateArticleModal.jsx` into a fully interactive WYSIWYG rich text engine. Implemented `contentEditable` container styled with `.rich-editor-content`, instant toolbar button actions (`Bold`, `Italic`, `Underline`, `Bullet List`, `Numbered List`) using `document.execCommand`, keyboard shortcuts (`Ctrl+B`, `Ctrl+I`, `Ctrl+U`), `onMouseDown={(e) => e.preventDefault()}` selection preservation, real-time active button state tracking via `document.queryCommandState`, empty state placeholder overlay, modal cleanup on open/close, and seamless rich HTML (`ContentHtml`) persistence to backend API and SQL Server database. Verified via live frontend build and API integration script.
- Phase 50 (Admin Console & Governance Moderation Workable Refactor) — Refactored `AdminConsole.jsx` to be 100% workable. Implemented permanent preview modal footer with status-aware resolution audit trail, direct Article content rendering with categories & images, validation-compliant report resolution (`PUT /api/interactions/reports/{id}/resolve` for Dismissed, Removed Content, Reinstated Content), searchable interactive employee picker with reason presets & duration options for user suspension (`PUT /api/users/{id}/suspend`), two-way state synchronization across `usersList` and `UserContext`, top search bar synchronization across all admin tabs, live community channels, and local-storage-backed AI moderation controls. Verified via backend API scratch test and frontend build.
- Phase 51 (Home Feed Multi-Attachment & Scheduled Post Display Fix) — Resolved root cause where multiple files attached to scheduled posts failed to render on the Home page (`Dashboard.jsx`), previously showing broken fallback SVG "Knome Enterprise Media Attachment". Extended backend `FeedItemDto` with `Attachments`, `AttachmentUrls`, `ContentText`, and `Status`. Included author's scheduled posts in `FeedRepository.GetCandidatePostsAsync`. Added extension-based `detectFileType` in `apiService.js` and upgraded `mapFeedItem` to map all attached documents, videos, audios, and images. Hardened `PostCard.jsx` to resolve media URLs across all media players and doc previewers. Verified via live API tests against SQL Server.
- Phase 52 (Post Audiences & Multi-Target Notification System) — Implemented complete audience management system for Create Post (Everyone, Specific Community, Specific Person/Connections) alongside global laptop local time synchronization (`UtcDateTimeJsonConverter`). In `PostService.cs`, added notification generation across all 3 modes: broadcast to all active colleagues for Everyone (`NotificationTypes.HrAnnouncement`), targeted dispatch to all approved members (`Status == 'Approved' || Status == 'Active'`) and creator for Specific Community (`NotificationTypes.Community`), and direct delivery to targeted user IDs with strict feed privacy for Specific Connections (`NotificationTypes.Share`). Enriched `FeedItemDto` and `apiService.js` to map `communityId`, `communityName`, `sharedWithName`, and `audienceUserIds`, rendering dynamic blue community badges and purple direct recipient badges. Verified 100% PASS via automated integration script against live SQL Server.
- Phase 53 (Community-Named Welcome Message & Real-Time Interactions) — Dynamic attribution and real-time interaction refactor for community feeds. Replaced the static hardcoded fallback authored by "Loveneesh Sharma" with dynamically generated welcome posts in the name of the active community (`activeCommName`, role "Official Community Space", pinned), and auto-sanitized legacy seed posts. Activated full real-time interactivity across post action bars: instant optimistic Likes with visual fill and `knome:reaction-updated` broadcast; expandable inline Comments drawer with live comment submissions, instant comment count increments, and restricted word validation; and clickable Share button with direct link copy to clipboard, `ArticleShareModal` opening, and real-time share count updates. Verified via frontend build and API sync.
- Phase 54 (Real-Time Karma Points Update on Post Publication) — Fixed root causes of static Karma point balances upon post publishing. Elevated `KarmaCaps.CreatePostDailyCap` in backend `KarmaConstants.cs` from 10 to 50 points so regular publishing is never choked by testing thresholds. Integrated global window event `karma-updated` in `awardRuleKarma` and introduced `refreshKarma` backend synchronizer. Upgraded `Navbar.jsx` with live listeners for `karma-updated`, `post-created`, and `article-created` and removed `!isSysAdmin` filter so all users (employees and admins) see their points and badge in the top bar. Added live reactive stats in `Sidebar.jsx` (Points, Posts, Followers) and wired post publishing karma awarding across `CreatePostModal.jsx`, `CommunityView.jsx`, and `PostCard.jsx` (Publish Now). Fully verified via live integration test against SQL Server (+2 pts per post).
- Phase 55 (Trending Tags Real-Time Hashtag Matching & Post Deep-Linking) — Resolved disconnected Trending Tags on Dashboard and the `Found 0 results for "#MPOnline"` defect. In `SearchRepository.cs`, implemented `GetQueryTerms` to normalize hashtag queries (stripping `#` to match both `#tag` and `tag` across Posts, Articles, Videos, Communities, Jobs), expanded post search audience to include `Community` posts alongside `Everyone`, enabled tag matching against post content text, and projected informative post snippet titles. In `TrendingTagsWidget.jsx`, dynamically extracts and ranks real hashtags from live posts and feed items with post count badges (e.g. `#DotNet 5`, `#MPOnline 5`, `#devops 8`). In `Search.jsx`, routed post clicks directly to `/posts?id=${id}` and rendered fallback author titles. In `Posts.jsx`, added URL query param support (`?tag=...`, `?q=...`) for deep-link filtering. In `PostCard.jsx`, made inline hashtags clickable to search. Verified with 100% hits across 6 test hashtags via live script and frontend build.
- Phase 56 (Community Welcome Card Cleanup & Rules/FAQ Persistence Engine) — Addressed clutter on official community introduction cards and rules/FAQ persistence bugs in `CommunityView.jsx`. Cleaned up the Welcome Post card by removing regular post moderation buttons (`unpin`, `delete`, `suspend`) and engagement action bar (`like`, `comment`, `share`, `open post`, inline comments drawer), rendering it as an official verified space welcome announcement. Removed broken `person_off` suspension button from post headers. Added an interactive Community Rules & FAQ manager into the Admin Tools tab with add/edit/delete capabilities. Persisted Rules and FAQs to backend SQL Server (`communitiesApi.update`), dedicated local storage (`knome_community_rules_faq_${targetId}`), custom community metadata, and live component state. Added quick "Edit" shortcut buttons in sidebar Rules & FAQ cards for community admins. Verified clean frontend build and responsive UI.
- Phase 57 (Connection Deduplication, Self-Exclusion & Sent Requests Workflow) — Resolved duplicate users and self-user display in People Network (`Network.jsx`). Implemented robust `isCurrentUser`, `isSameUser`, and enhanced `deduplicateUsers` with ID, employee code, and normalized name filtering (stripping parenthesized suffixes) so logged-in users (e.g. Loveneesh Sharma) and duplicate roster entries never appear in Suggestions or Connections. Rewired `handleConnect` so sending a connection request immediately removes the person from "People You May Know" and moves them into the **Connection Requests** tab under **Sent Connection Requests Pending**. Synchronized tab header badges with separate pills for received requests and sent requests, and added sub-filter pills (`All`, `Received`, `Sent Requests`) in the Connection Requests tab. Verified clean frontend and backend builds.
- Phase 58 (Removal of Email Digests & Workable In-App Notification Preferences) — In compliance with architecture constraints deferring email integrations, completely removed the Email column and Email Digests section from `NotificationSettingsModal.jsx` and the Email Digest toggle from `AdminConsole.jsx`. Overhauled `NotificationSettingsModal.jsx` into a modern in-app preference manager with individual toggles for 7 key event types (Comments, Reactions, Followers, Community Invites, Mentions, Community Posts, Job Postings), quick Enable All/Mute All buttons, and per-user `localStorage` persistence. Connected preferences into `Navbar.jsx` to dynamically filter displayed notifications, exclude muted alerts from the unread bell counter, and suppress real-time pop-up toasts and audio chimes for muted categories. Verified via clean frontend and backend production builds.
- Phase 59 (Scroll-Wise Infinite Loading Across Catalog Pages) — Engineered reusable `useScrollLoading` hook with window scroll listener, batch increments, and filter-change reset. Created dark-mode aware `ScrollLoadingIndicator` badge. Wired progressive scroll loading across 8 main catalog views (`Dashboard`, `Articles`, `Videos`, `Podcasts`, `Communities`, `Jobs`, `Network`, `Search`), maintaining sub-second initial page paints and zero layout shifts. Verified via production Vite bundle.
- Phase 60 (Attractive Modern UI Design System Upgrade) — Transformed visual presentation to a modern, attractive enterprise SaaS design system with zero functional disturbance. Upgraded light/dark design tokens in `index.css` (clean slate-50 and deep cosmic obsidian `#070a13`), enhanced glassmorphism (`.glass-premium`), added tech dot-grid texture (`.tech-dots-pattern`), and vibrant CTA button gradients. Overhauled `Layout.jsx` with multi-tiered atmospheric ambient lighting (indigo, cyan, purple). Refined `Navbar.jsx` (golden karma badge, modern glass theme toggle and notification bell, gradient avatar ring), `Sidebar.jsx` (decorative banner, mini stat tiles, radiant Create Post CTA), and `Dashboard.jsx` (refined header with live weekday status chip, elevated post composer with icon tiles). Verified with clean 757ms production build.
- Phase 61 (Professional Knome Branding & Portal Label Removal) — Upgraded Knome branding assets with an ultra-premium 3D faceted crystal emblem, removed the redundant "PORTAL" subtitle across navigation elements and buttons, optimized desktop sidebar width to 260px with font-black styling to eliminate name wrapping/truncation, and polished the navigation menu with modern pill indicators.
- Phase 62 (Global Display of Employee Name & Designation Instead of Employee ID) — Replaced all user-facing instances of raw employee codes (`MP0108`, `MPO101`, `EMP001`, `mpo652`) with the employee's full name, designation, or department across Profile headers, contact modals, community member cards, share recipient selectors, role pending guards, suspension overlays, and admin governance views. Built-in automatic employee ID to full-name resolution safeguard via `resolveEmployeeName` and `KNOWN_ROSTER_NAMES` in `UserContext.jsx`. Verified with 769ms production build and direct IIS sync.
- [x] Phase 63 (Database Purge of Obsolete Test Employees) — Purged 4 obsolete test/duplicate users (`UserId 1067` MPO101, `1068` NON_EXISTENT_999, `1069` MPO664, `1071` MPO107) from the live SQL Server `Knome` database across all 32 foreign key dependencies. Reassigned governance references (`ModerationReports`, `AuditLog`, `Communities`) to primary System Administrator (`UserId 1`, `MP0108`), deleted child records across `Notifications`, `KarmaBalances`, `KarmaTransactions`, `UserRoles`, and `UserCredentials`. Synchronized frontend rosters and verified with live `sqlcmd` query and backend build.
- [x] Phase 64 (Clean Shared Post Quote Card Rendering & Raw URL Stripping) — Fixed shared post rendering in Profile and Community feeds. Stripped redundant automated headers (`Shared Post: "..."`) and internal raw URLs (`http://localhost:5173/posts?id=...`) from the body paragraph so only authentic user commentary is displayed. Upgraded shared post embeds into modern, LinkedIn/Twitter-style Quote Cards with subtle left borders, quote typography, `Shared Post` chips, post IDs, author attribution, and "Open Post ↗" actions. Expanded regex to capture both numeric and alphanumeric post IDs (e.g. `welcome_1788943687`). Verified via production build and IIS sync.
- [x] Phase 65 (Brand Logo Integration With Full KNOME Wordmark) — Extracted and integrated high-resolution transparent brandmark assets from user-provided source image (`media_1789110616941.jpg`). Generated light mode (`knome_logo.png`, navy text `#0B132B`), dark mode (`knome_logo_dark.png`, white text `#F8FAFC`), standalone emblem (`knome_k_emblem.png`, 214x224), and favicon (`favicon.png`, `knome_emblem.png`). Integrated dynamic theme-aware logo in `Navbar.jsx`, `Login.jsx` branding panel, `Footer.jsx`, and `index.html`. Verified via production Vite build and IIS synchronization.
- [x] Phase 66 (Compact Enterprise Footer Redesign & Size Reduction) — Completely redesigned `Footer.jsx` to reduce vertical height by ~70% (from ~260px down to ~70px). Refactored layout into a sleek, 3-column responsive bar with MPOnline logo centered (`lg:absolute lg:left-1/2 lg:-translate-x-1/2`). Refined `knome_logo_dark.png` to eliminate speckled text noise with pure white anti-aliased typography. Replaced oversized white MPOnline slab with an elegant compact badge (`max-h-[20px]`). Preserved all core touchpoints (Knome brand logo, tagline, 5 social buttons, direct clickable customer care/HR support/email links, Bhopal address, and copyright). Verified via production Vite build and deployed to IIS.
- [x] Phase 67 (Direct Knome Access & Login Screen Removal) — Completely removed the login screen, credentials input, and quick demo accounts card. Pre-authenticated `UserContext` with default System Administrator identity (`Loveneesh Sharma`, `MP0108` / `SYSADM`) with `isAuthenticated: true` from the first frame. Updated `AuthGuard.jsx` to eliminate login redirects. Configured `App.jsx` to route `/login` directly to `/`. Verified Knome.API backend is running on `http://localhost:5095` and deployed production build to IIS.
- [x] Phase 68 (Moderation Table Header Score Refinement) — Updated the Moderation Reports table column header in `AdminConsole.jsx` from `AI Score` to `Score` with `whitespace-nowrap` to prevent awkward two-line vertical wrapping. Verified via production Vite build and deployed to IIS.
- [x] Phase 69 (Community Default Type Renamed to Org) — Standardized organization-wide community badges across `Communities.jsx`, `Profile.jsx`, and `AdminConsole.jsx` from `DEFAULT` / `DEFAULT (ORG)` to `ORG`. Added `formatCommunityType` normalizer and updated filter pills to `Org`. Verified via production Vite build and deployed to IIS.
- [x] Phase 70 (Community Moderator Replaced by Community Administrator) — Standardized and replaced all occurrences of "Community Moderator" with "Community Administrator" across the SQL Server database (`CommunityMembers` table), backend API constants (`CommunityMemberTypes.Admin`), service assignments (`CommunityService.cs`), and frontend components (`CommunityView.jsx`, `AdminConsole.jsx`). Updated member role badges to the purple Community Administrator style, updated action toggle buttons (`Make Administrator` / `Set as Member`), cleaned FAQ text, and deployed production build to IIS.
- [x] Phase 71 (Universal Scroll-Wise Loading Across All Feeds, Catalogs & Modals) — Extended infinite scroll loading (`useScrollLoading` hook + `ScrollLoadingIndicator`) across all remaining views in the platform: Posts feed (`Posts.jsx`), Saved Content catalog (`SavedContent.jsx`), Community View (`CommunityView.jsx` Posts, Members, Files tabs), User Profile tabs (`Profile.jsx` Posts, Articles, Videos, Podcasts, Communities, Network), Admin Console (`AdminConsole.jsx` Moderation Reports, User Governance, Audit Trail tables), and Create Community Modal (`CreateCommunityModal.jsx` employee grid using containerRef). Verified via production Vite build and deployed to IIS (`C:\inetpub\wwwroot\knome\`).
- [x] Phase 72 (Remove Or Undo Under HR Review Community Submission) — Implemented feature allowing users to withdraw or cancel a pending community creation request before HR review decision.
- [x] Phase 73 (Logout Redirect To MPO Employee Hub) — Configured platform logout flows to smoothly redirect users back to the MPOnline Employee Hub portal.
- [x] Phase 74 (Fix React Hooks Order In CommunityView) — Resolved conditional early-return hook violation in `CommunityView.jsx` by standardizing execution order across all 64+ hooks.
- [x] Phase 75 (Community Admin Protection & Member Management Modals) — Protected sole Community Administrators from accidental removal or suspension until another member is promoted to Community Administrator. Replaced demotion button by removing "Set as Member" for Community Admins. Implemented dedicated Suspend Member duration selection modal (24h, 3d, 7d, 14d, 30d, custom date, indefinite) with reason tracking, and added explicit Member Removal confirmation modal. Deployed to IIS.
- [x] Phase 77 (Openings Module Clean Copywriting & Database Terminology Removal) — Cleaned up all developer database jargon from the Openings (`/jobs`) module. Removed `(MS SQL Database Wired)` from the hero badge, polished hero description to highlight MPOnline career advancement, replaced loading state text with 'Loading opportunities...', and refined `CreateJobModal` copy and publish icon. Production frontend build verified in 1.73s (0 errors). Deployed to IIS.
- [x] Phase 78 (User Status Indicators: Active, Suspended, Inactive) — Implemented unified account status indicators (`Active`, `Suspended`, `Inactive`) across user profile cards, navigation bar triggers/menus, profile headers, and administrative governance tables with responsive status badges and reactive avatar dots. Deployed to IIS.
- [x] Phase 79 (Saved Content: Reposition All Types Tabs Above Category Folders) — Repositioned the content types filter bar ("All Types", "Posts", "Articles", "Videos", "Podcasts" and search/sort controls) directly above the Category Folders bar on the Saved Content page. Deployed to IIS.
- [x] Phase 80 (HR Analytics Copywriting & KPI Metric Card Ordering) — Purged all database/SQL keywords from the HR Analytics portal (hero badge, subtitle, loading message, community health, karma ranks, audit logs). Reordered RPT-01 KPI metric cards into Active Users -> Suspended Users -> Total Registered Users, and formatted Karma Points to show Total Karma Points first with Average Karma Per User as subtext. Deployed to IIS.
- [x] Phase 81 (Navbar Karma Points Badge Activation for HR & Community Admins) — Enabled Karma Points pill badge in top navbar for HR Administrators and Community Administrators. Removed hardcoded employee ID exemptions, allowed Karma History access, and enabled points in Profile and Admin Console. Deployed to IIS.
- [x] Phase 82 (Karma Points Level Table Addition Under "How to Earn Karma") — Added dedicated Karma Points Level Table directly beneath "How to Earn Karma" in `KarmaHistory.jsx`. Displays all 5 tier thresholds (Starter, Bronze, Silver, Gold, Platinum), titles, point ranges, and badges with automatic active tier highlighting. Deployed to IIS.
- [x] Phase 83 (Karma Points Page Branded Loading State) — Added a branded golden loader with spinning accent, military medal badge icon, and descriptive status text while karma balances, tiers, and leaderboard rankings are loading in `KarmaHistory.jsx`. Deployed to IIS.
- [x] Phase 84 (Karma Points Level Table Responsive Width & Clipping Prevention) — Expanded right column to `xl:col-span-5` (41.67%) and enforced `table-fixed` layout with `38% / 32% / 30%` colgroup percentages in `KarmaHistory.jsx` so the entire table, headers, and badges fit without right-edge cutoff. Deployed to IIS.
- [x] Phase 85 (Universal Suspend Modal, Anti-Clipping Responsive Layout, and Strict Login Blocking) — Unified all account and membership suspension actions across Knome into a single standardized, modern popup modal (`SuspendUserModal.jsx`) matching the executive design specifications. Built with anti-clipping responsive flex architecture (`max-h-[90vh] flex flex-col`, pinned header, pinned footer, internal scrollable body) to guarantee zero header/title clipping on any laptop screen size. Wired seamlessly across Community Management, User Governance table, Disciplinary quick actions, Moderation Reports, and User Details. Enforced strict multi-tier login rejection in backend (`AuthService.cs` `GetCurrentUserAsync` and `GetCurrentUserByIdentifierAsync` throw 403 Forbidden on suspension), `AuthGuard.jsx` (strictly renders full-screen Access Denied with restricted module notice), and `UserContext.jsx` (blocks fallback to active session, wipes JWT tokens, and marks suspended status). Built and deployed to IIS.
- [x] Phase 86 (Posts Section Hot Posts, Trending Tags, and Read More Expanders) — Embedded the `HotPostsWidget` and `TrendingTagsWidget` in the right sidebar of the Posts page (`/posts`), with direct tag click filtering and a `🔥 Hot Posts` quick filter button. Implemented 2-line text clamping with interactive `Read more` / `Show less` expander on post commentary, and image gallery truncation with `Read more (+N more photos)` button after 4 images that expands into a full photo grid. Built and deployed to IIS.
- [x] Phase 87 (Moderation Reports Reporter Identity Separation and Content Visibility) — Resolved reporter identity conflation bug where `ReportedUserName` and `ReportedUserId` mistakenly copied `ReporterFullName` / `ReporterUserId` when posts lacked live DB references. Seeded `Post #10050` (author `Rohan Verma`, User #1065) and `Post #10048` (`Aarav Sharma`, User #1063). Fixed `ContentInteractionService.cs` fallbacks across all 4 content types. In `AdminConsole.jsx`, removed reporter fallbacks in `getFallbackPostContent`, `fetchReports`, and `handleOpenPostPreview`, ensuring reported user displays `Rohan Verma` with `"Reported Author"` badge, reporter card displays `Vilash Deshmukh`, and Suspend User action strictly targets the reported author. Restored original harassment and policy content text in modal preview body and table snippets. Built and deployed to IIS.
- [x] Phase 88 (Report Preview Modal Stacking & Clipping Fix, Content Visibility & Author Resolution) — Fixed Report Preview modal clipping behind top navigation bar by removing parent `z-10` stacking context trap in `Layout.jsx` and portaling all modals (`SuspendUserModal`, Modal 2 Manage Roles, Modal 3 Report Preview, Modal 4 Manage Community, Preview Media, User Details) to `document.body` via `createPortal` with `z-[9999]`. Redesigned Report Preview modal layout with pinned header (`sticky top-0`), internal scrollable body (`flex-1 min-h-0 overflow-y-auto`), pinned footer (`sticky bottom-0`), and balanced media dimensions (images `h-36 sm:h-40`, videos `max-h-48`) so audio players and document attachments remain immediately visible. Resolved author names (e.g. `Loveneesh Sharma`) and updated Post #10130 content in SQL Server `[dbo].[Posts]` to realistic spam notice. Frontend build in 879ms (0 errors), deployed to IIS.
- [x] Phase 89 (Role-Based FAQ Guide Handbook & Navbar Action Button) — Added global FAQ button in top-right corner of `Navbar.jsx` with help icon and secondary user menu entry. Built `RoleFaqModal.jsx` with automatic active role detection, interactive role selector tabs (Employee, Community Admin, HR Admin, System Admin), search bar, accordion Q&A, and comprehensive guidance on using Knome properly. Frontend build in 1.69s (0 errors), deployed to IIS.
- [x] Phase 90 (Role-Restricted FAQ Access & System Administrator Rule Editor) — Gated FAQ visibility so standard roles (Employee, Community Admin, HR Admin) see only their own verified FAQ with locked single-role indicator. System Administrators can view all role tabs and edit/add/delete FAQ questions and role rules with persistent storage and real-time syncing. Frontend build in 1.34s (0 errors), deployed to IIS.
- [x] Phase 91 (Moderation Metric Cards Color & Naming Alignment) — Renamed metric cards in Admin Console: Pending Queue ➔ Pending Reports, Action Taken ➔ Removed Reports (red-themed styling), and Dismissed ➔ Dismissed Reports (green-themed styling). Aligned table status badge and analytics breakdown colors. Frontend build in 1.27s (0 errors), deployed to IIS.
- [x] Phase 92 (Navbar Profile Menu Cleanup & People You May Know Connection Icon Update) — Removed redundant FAQ item above "My Profile" inside the user avatar dropdown menu (preserving the top-right header `? FAQ` button). Updated the mutual connection icon in `PeopleYouMayKnowWidget.jsx` from `hub` to the standard, professional `group` icon (two colleagues silhouette). Frontend build in 858ms (0 errors), deployed to IIS.
- [x] Phase 93 (Community Card Details Anti-Clipping & Discover All Communities Display) — Expanded grid layout from cramped 4-column to spacious 3-column on desktop (`lg:grid-cols-3 2xl:grid-cols-4`), completely eliminating text clipping in community card footers (`0 posts`, member count, category badges) with `whitespace-nowrap` and `mt-auto`. Rendered all communities (13+) directly on the Discover tab without initial 8-item slicing. Frontend build in 886ms (0 errors), deployed to IIS.
- [x] Phase 94 (Community Catalog Synchronization, Compact Card Sizing & Delete Button Relocation) — Reconciled enterprise channels between Admin Console and Discover tab via unified `DEFAULT_ENTERPRISE_COMMUNITIES` data source. Compacted community cards by ~40% (4 columns, ~100px banners, tight padding and typography). Relocated red administrative delete button to the bottom-right of the card footer. Frontend build in 1.15s (0 errors), deployed to IIS.
- [x] Phase 95 (Administrator Terminology Simplification to Admin & Full Employee Name Display) — Standardized user-facing terminology from "Administrator" to "Admin" across community role badges, action buttons, modals, and user cards. Removed `truncate` to display full employee names without cutoff across Community Members, User Cards, and Directories. Frontend build in 1.25s (0 errors), deployed to IIS.
- [x] Phase 96 (Community Approval Queue Integration in Admin Console & Catalog) — Fixed missing community creation approvals on the Admin Page (`/admin-console`) and Communities catalog (`/communities`). Added dedicated Community Approvals tab and notice banner in Admin Console, normalized `isHRorAdmin` and `isSysAdmin` across all admin roles, and enabled Admin Console access for HR Admins. Frontend build in 1.31s (0 errors), deployed to IIS.
- [x] Phase 97 (Community Module 'Default' to 'Org' Terminology Standardization) — Standardized all community references from "Default", "Default (Org)", and "Organization Default" to "Org" / "Org Space" across creation modals, detail pages, management dropdowns, leave restriction toasts, and FAQ guides. Added backend API support for "Org" alongside "Default" in `CommunityConstants.cs`, `CommunityService.cs`, `CommunityRepository.cs`, and `UserService.cs`. Frontend build in 1.29s (0 errors), backend build succeeded (0 errors), deployed to IIS.
- [x] Phase 98 (Community Member List Anti-Clipping Layout & Employee Name Resolution) — Resolved raw employee IDs (e.g. `EMP004`) to verified employee names (`Neha Gupta`), added dedicated employee ID monospace badges (`[EMP004]`), capitalized designations and departments, and eliminated text wrapping/squeezing in the Community Members list by restructuring row layout into a spacious, responsive `flex-col xl:flex-row` configuration with full width for member information on viewports `< 1280px`. Frontend build in 1.92s (0 errors), deployed to IIS (`C:\inetpub\wwwroot\knome\`).
- [x] Phase 99 (Community-Level Suspension Isolation & System Admin Lockout Message) — Isolated community suspension scope so suspending a member from a community blocks their access to that community only (displaying a dedicated "You are suspended from this community" screen) while keeping their Knome account active. Enforced platform-wide lockout when suspended by System Administrator with the exact message: "Your account is suspended by system admin". Frontend build in 835ms (0 errors), backend build verified (0 errors), deployed to IIS (`C:\inetpub\wwwroot\knome\`).
- [x] Phase 107 (Role-Based User Manual, Strict Role Isolation & PDF Export in FAQ Modal) — Added comprehensive Standard Operating Procedure (SOP) operational user manuals inside the FAQ modal (`RoleFaqModal.jsx` and `roleManualsData.js`) covering profile governance, publishing, scheduling, articles, videos/podcasts, communities, karma points matrix, and workplace ethics. Enforced strict role isolation so standard employees only access their assigned role's manual & FAQs, while System Admins retain master switching capabilities. Implemented vector-rendered printable PDF manual generation (`printRoleManualPdf`) with A4 print stylesheets and prominent "Download PDF Manual" buttons. Frontend production build in 821ms (0 errors), deployed to IIS (`C:\inetpub\wwwroot\knome\`).
- [x] Phase 108 (Role Terminology Simplification from 'Administrator' to 'Admin') — Simplified user-facing role terminology from "Administrator" to "Admin" across Admin Console Manage Roles modal, governance forms, and community components. Normalized role matching to ensure checkmarks accurately highlight active roles. Frontend and backend builds verified with 0 errors, deployed to IIS.
- [x] Phase 109 (Community Member Deduplication & Single Unique Entry Resolution) — Eliminated duplicate member records (such as Deepak Simrodia appearing twice with "Employee" and "Software Developer" designations) in the Community Members & Roles tab. Implemented `deduplicateMembers` in `UserContext.jsx`, tightened `isMemberInList` checks in `CommunityView.jsx`, cleaned up cached storage automatically, and enforced unique member rendering in `filteredMembers`. Frontend build verified in 1.28s (0 errors), deployed to IIS.
- [x] Phase 110 (Search Hero Badge YouTube Reference Removal & Enterprise Tag Line Refinement) — Removed external branding reference "✨ UNIVERSAL YOUTUBE-STYLE SEARCH" from `Search.jsx` hero section and updated the pill tag line to "✨ Universal Enterprise Search" matching Knome's clean enterprise identity. Sanitized internal code comments in `Navbar.jsx`. Built and deployed to IIS webroot.
- [x] Phase 111 (Profile Interests Icon Replacement from 'favorite' to 'interests') — Replaced informal heart symbol `favorite` with the official Google Material Symbol `interests` on the profile Interests card in `Profile.jsx`. Built and deployed to IIS webroot.
- [x] Phase 112 (Removal of 'Rich WYSIWYG' / 'WYSIWYG' Terminology Across Role Manuals & FAQ Modal) — Removed technical jargon "Rich WYSIWYG" and "WYSIWYG" from the Employee scope key focus pill badges in `RoleFaqModal.jsx` and across standard operating procedures in `roleManualsData.js`. Built and deployed to IIS webroot.
- [x] Phase 113 (Removal of Technical 'RPT-01' through 'RPT-05' Codes from Analytics & Reporting Portal) — Removed report code badges from tab headers in `HRAnalytics.jsx` and replaced them with polished Material Symbol category icons. Built and deployed to IIS webroot.
- [x] Phase 114 (Database Purge of Obsolete Test Users 1073 & 1075) — Atomically purged obsolete test records `UserId 1073` (`MPO664`) and `UserId 1075` (`MPO101`) along with dependent child records across `KarmaTransactions`, `KarmaBalances`, `UserRoles`, and `UserCredentials` from SQL Server database `Knome`.
- [x] Phase 115 (Update User 1076 with Official MPO Employee Hub Profile) — Synchronized `UserId 1076` with Vishendra Sharma's official MPO Employee Hub profile (`EmployeeId: MP0664`, `FullName: Vishendra Sharma`, `Email: vishendra.sharma@mponline.gov.in`, `Designation: Track Lead`, `Department: Higher Education`). Cleaned up legacy test account `1061`, reassigning connections and notifications. Updated backend `AuthService.cs` with `MP0`/`MPO` alternate search matching.
- [x] Phase 116 (Hide Admin Console from HR Admin Sidebar Navigation) — Restricted Admin Console (`/admin-console`) in `Sidebar.jsx` exclusively to System Administrators (`isSysAdmin`), hiding it from the navigation sidebar for HR Administrators while keeping **HR Analytics** readily accessible. Built and deployed to IIS webroot.
- [x] Phase 117 (Database & Backend Timestamp Alignment to Indian Standard Time) — Replaced 27 SQL Server default constraints with `sysdatetime()`, converted all historical database timestamps by +330 minutes to authentic Indian Standard Time (IST, UTC+05:30), standardized backend timestamps across 15+ services and repositories using `KnomeTime.Now`, and updated frontend `parseLaptopDate` to parse local time directly. Row 10113 now shows `12:06:11` matching local laptop clock.
- [x] Phase 118 (Remove Default Community Pre-selection in Share Modals) — Removed automatic pre-selection of the first community (`Java` / index 0) when opening the "Share to Community" dialog across `ArticleShareModal.jsx`, `ShareProfileModal.jsx`, `Videos.jsx`, and `VideoPlayerModal.jsx`. Modal now initializes with no community selected, keeps the submission button disabled until explicit user selection, hides the "Selected: ..." badge until chosen, and enables toggle select/unselect on community cards.
- [x] Phase 119 (Shared Article, Video, Podcast & Post Direct Redirection In Community Feed) — Implemented centralized target resolver (`resolveSharedTarget`) in `apiService.js` to inspect shared URLs and metadata across articles, videos, podcasts, profiles, and posts. Upgraded `CommunityView.jsx` quote cards and bottom action buttons to dynamically link directly to `/article-view?id=${id}` for articles, `/videos?id=${id}` for videos, etc., with dynamic badges (`Shared Article`, `Shared Video`), action labels (`Read Article`, `Watch Video`), and tooltips. Resolved legacy shared article `#10050` ("Best Practices for Building Scalable REST APIs with ASP.NET Core"). Updated `PostCard.jsx` quote card, action menu, and dynamic share modal content type. Built and deployed to IIS.
- [x] Phase 120 (Private Community Discovery & Target Reference Scope Fix) — Resolved uncaught `ReferenceError: target is not defined` in `CommunityView.jsx` by lifting `resolveSharedTarget` into the post mapping scope, ensuring the action bar buttons properly access `target`. Removed premature 403 authorization lock from `GetCommunityAsync` in `CommunityService.cs` so non-members can discover private communities, view headers and descriptions, and click "Request to Join" (`FR-CM-02`), while keeping private post feeds and member rosters strictly protected. Added private community lock banners to feed, members, and files tabs. Built and deployed to IIS.
- [x] Phase 121 (Resolve Frontend 401 Unauthorized Cascades & Fast Session Restoration) — Resolved cascade of HTTP 401 Unauthorized errors across Dashboard widgets (`HotPostsWidget`, `Navbar`, `Dashboard`, `Posts`, `Communities`, `Karma`) caused by token expiration and a concurrency race condition in `apiClient.js`. Removed the `!reauthPromise` barrier so all concurrent 401 requests await the deduplicated `silentReauth()` promise and retry cleanly with the fresh token. Initialized `isAuthLoading` in `UserContext.jsx` to shield child components from mounting prematurely with stale tokens. Prioritized local BCrypt verification in `AuthService.cs` and corrected MPO scope to `openid offline_access`, slashing login response time from 4,438ms to 346ms (92% speedup). Verified live with all 7 previously failing endpoints returning HTTP 200 OK. Built and deployed to IIS.
- [x] Phase 122 (Admin Console "+ Create Channel" Action Activation) — Activated the "+ Create Channel / New Community" action card in Admin Console (`/admin-console`) and added a dedicated "+ Create Channel" button directly on the community moderation toolbar. Connected to `CreateCommunityModal.jsx` via `createPortal(..., document.body)` with auto-closing, immediate channel list refresh (`fetchCommunities()`), pending approval count synchronization (`refreshPendingCommunityApprovals()`), duplicate channel name prevention via `existingCommunities={communityChannels}`, and toast notifications. Frontend built with Vite (0 errors) and deployed to IIS.
- [x] Phase 123 (Suspended User Login Alert and Logout Workflow) — Implemented mandatory suspension popup dialog (`SuspendedAlertModal.jsx`) when an employee suspended from the Admin Console attempts to log in (SSO, session restore, direct access, or user switcher). Displays alert: *"Your account is suspended by system admin"*, user name, employee ID, and contact guidance. Disables timer auto-dismissal. Requires explicit user acknowledgment via prominent **"OK"** button, which terminates authentication, clears session tokens, and executes full logout redirect to `https://counselling-1.mponline.demo.gov.in:3001/login?logout=true`. Built and deployed to IIS.
- [x] Phase 124 (Remove Clear All Button From Notifications Dropdown) — Removed the "Clear all" button and separator bullet from the top navbar notification popover header (`Navbar.jsx`), ensuring unread and recent notification history is preserved cleanly while keeping "Mark read" and notification preferences accessible. Built and deployed to IIS.
- [x] Phase 125 (Remove Recommended Filter From Everywhere) — Completely removed the "Recommended" filter buttons, tabs, and recommendation override branches across Posts feed (`Posts.jsx`), Articles directory (`Articles.jsx`), Podcasts catalog (`Podcasts.jsx`), and Videos hub (`Videos.jsx`), standardizing all platform views on authentic chronological and category filtering. Built and deployed to IIS.
- [x] Phase 126 (Rename Podcast to Audio, Audio Symbol Update, and LinkedIn-Style Article Editor Toolbar) — Replaced "Podcast" word with "Audio" and microphone symbol with "audiotrack" symbol across article creation attachments (`Articles.jsx`), quick links (`Sidebar.jsx`), saved content tabs (`SavedContent.jsx`), profile tabs (`Profile.jsx`), search filter chips (`Search.jsx`), and audio upload modals. Completely overhauled the rich text editor formatting toolbar in `Articles.jsx` and `CreateArticleModal.jsx` to match LinkedIn's article editor toolbar: Style ▾ dropdown (Title H1, Heading 1 H2, Heading 2 H3, Normal P), Bold B, Italic I, bulleted list, numbered list, blockquote ”, code block {}, horizontal divider —, link, code snippet </>, and image upload. Built and deployed to IIS.
- [x] Phase 127 (Remove Active Modules Label From Admin Console) — Removed the redundant "Active Modules:" text label from the sub-module navigation bar in `AdminConsole.jsx`, keeping the functional navigation buttons (User Directory, Role Requests, etc.) intact with clean visual alignment under the Domain selector cards. Built and deployed to IIS.
- [x] Phase 128 (Remove Style Dropdown, Code Block, and Code Snippet Buttons from Article Editor) — Removed the `Style ▾` dropdown button, `Code Block` (`{}`) button, and `Code Snippet` (`</>`) button from both the full-page article creator (`Articles.jsx`) and modal article editor (`CreateArticleModal.jsx`), leaving a clean, balanced rich text toolbar for enterprise article authoring. Built and deployed to IIS.
- [x] Phase 129 (Remove Community Approvals and AI Moderation Rules Buttons from Admin Console) — Removed the `Community Approvals` and `AI Moderation Rules` buttons from the Domain 1 (`Moderation & Content Safety`) navigation sub-tab bar in `AdminConsole.jsx`, updating the pending badge and subtitle accordingly. Built and deployed to IIS.
- [x] Phase 130 (Remove User Directory and Role Requests Buttons from Admin Console) — Removed both `User Directory` and `Role Requests` sub-module navigation buttons from the Domain 2 (`User & Access Governance`) navigation bar in `AdminConsole.jsx`, and prevented rendering an empty Tier 2 bar when Domain 2 is active. Built and deployed to IIS.
- [x] Phase 131 (Remove System Parameters Button from Admin Console) — Removed the `System Parameters` sub-module navigation button from Domain 3 (`Operations & Compliance`) in `AdminConsole.jsx`, updated operations domain click action to default to Audit Trail (`audit`), and updated the domain card description. Built and deployed to IIS.
- [x] Phase 132 (Opening Module URL Redirection & MPO SSO Alignment) — Implemented dynamic host/port detection (`getSsoUrls()`) and automatic registered redirection to MPO Employee Hub login (`client_id=Knome-2026`, `returnUrl`, `redirect_uri`) in `Login.jsx`. Implemented global BFF signout flow in `UserContext.jsx` redirecting to `https://counselling-1.mponline.demo.gov.in:3001/api/bff/signout`. Routed unauthenticated visits in `AuthGuard.jsx` through `/login`. Aligned backend JWT `ValidIssuers` and signing keys. Fixed Openings (`/jobs`) module to unpack paged results, map `applicationLink`, normalize URLs, and ensure "Apply Now" redirects reliably to external URLs. Built and deployed to IIS.
- [x] Phase 133 (Revert Podcasts Page Audio Terminology Back to Podcasts) — Reverted "Publish Audio" back to "Publish Podcast" with the `podcasts` icon, reverted the hero description back to "Discover podcasts, tech talks...", reverted modal title in `UploadPodcastModal.jsx` back to "Publish Podcast Episode", and reverted the sidebar quick link to "Podcasts". Maintained "Audio" terminology in Article Editor attachments, Saved Content, Profile, and Search. Built and deployed to IIS.
- [x] Phase 134 (Remove Config Updates and Moderation Logs Cards from Admin Console) — Removed the `Config Updates` and `Moderation Logs` metric filter cards from the Audit Trail tab in `AdminConsole.jsx`, and balanced the grid layout across the remaining 3 cards (All Audit Events, User Governance, Batch Audits). Built and deployed to IIS.
- [x] Phase 135 (Remove Batch Audits Card from Admin Console) — Removed the `Batch Audits` metric filter card (`AIBatchAudit` / `smart_toy`) from the Audit Trail tab in `AdminConsole.jsx`, and balanced the grid layout to 2 columns (`grid-cols-1 sm:grid-cols-2`) for the remaining cards (All Audit Events, User Governance). Built and deployed to IIS.
- [x] Phase 136 (Remove Post Internal Job Button from Openings Page) — Removed the `+ Post Internal Job` call-to-action button and unused job creation modal state from the Openings / Opportunities page (`Jobs.jsx`). Built and deployed to IIS.
- [x] Phase 137 (Full MPO Employee Hub SSO Integration Alignment) — Completely aligned backend and frontend across all 6 core files: `appsettings.json` MPOAuthServer credentials, `MPOAuthServerSettings.cs` options model, `ServiceCollectionExtensions.cs` TokenValidationParameters (ValidateAudience false, ValidIssuers) and OnTokenValidated claim/role injection, `KnomeControllerBase.cs` GetCurrentUserId, `Login.jsx` automatic redirection to MPO Employee Hub login with client_id=Knome-2026 and returnUrl, `UserContext.jsx` SSO token extraction/preservation with profileApi.getMe and global BFF signout flow. Verified builds and deployed to IIS.
- [x] Phase 138 (Podcast In-Browser Recording & Direct Publishing Full Workflow) — Made the in-browser recording, preview playback, and episode publishing 100% fully functional end-to-end. Added `.webm`, `.ogg`, `.flac` support to `LocalFileStorageService.cs` so browser audio uploads are accepted. Added `CategoryName` to `CreatePodcastDto.cs` and automated category matching in `PodcastService.cs`. In `UploadPodcastModal.jsx`, fixed stale duration closure with `recordingTimeRef`, added audio playback preview with native `<audio controls>` player and "Discard & Record Again" actions, and enabled direct database publishing via `podcastsApi.create` for all authenticated employees. Enhanced `Podcasts.jsx` with instant optimistic updates on `podcast-published`. Built and deployed to IIS.
- [x] Phase 142 (Universal Real-Time Notification System — SignalR Full Coverage) — Complete implementation of the universal real-time notification engine across all enterprise modules. Backend: Updated `PublishDueScheduledPostsAsync` and `PublishDueScheduledArticlesAsync` to return `List<T>` for author notifications on scheduled content going live; added account activation/suspension notifications in `UserService.cs`; role workflow notifications (change/approve/reject) in `UserService.cs`; community join rejection notification in `CommunityService.DecideMembershipAsync`; community announcement broadcast to all active members in `CommunityService.PinPostAsync`; moderation advisory notification to content author in `ContentInteractionService.ResolveReportAsync`; regex-based `@mention` parsing and notification dispatch in `ContentInteractionService.AddCommentAsync`; enriched `NotificationService.EnrichNotificationDtoAsync` for `AdminBroadcast`/`RoleRequest` deep-link URLs. Frontend: Dynamic SignalR URL resolution (protocol/host-aware), `.withAutomaticReconnect([0, 2000, 5000, 10000, 30000])`, dual-tone harmonic Web Audio API chime (880Hz sine + 1320Hz triangle, zero file dependency), `99+` badge with pulse glow animation, IST-formatted toast timestamps via `formatNotificationDate`, and connection request inline action distinction (`follow_request` vs `connection_accepted`). Self-notification suppression enforced at both backend producer level and frontend `isSelfNotification` helper. `dotnet build` 0 errors; backend API healthy at `http://localhost:5095`; SignalR hub at `/hubs/notifications` verified in runtime logs.
- [x] Phase 146 (Role FAQ Modal Single Manual Download & FAQ-Only Focus) — Removed multi-tab manual view and sub-tab strip from `RoleFaqModal.jsx`, focusing modal on clear role FAQs and guidelines. Consolidated multiple PDF download buttons into a single prominent `Download User Manual` button in the header. Built and deployed to IIS.
- [x] Phase 147 (Knome Frequently Asked Questions Headline & Plain Language Upgrade) — Changed modal headline to `Knome Frequently Asked Questions` and rewrote all 21 questions and answers, role summaries, and topic badges into simple, friendly, easy-to-understand plain English. Incremented cache keys to `v3`. Built and deployed to IIS.
- [x] Phase 148 (Remove FAQ Header Subtitle & Role Navigation Note) — Removed "Simple answers and helpful guides for using Knome" subtitle from modal header and "(Helpful answers and guides for your role)" helper note from the role navigation bar in `RoleFaqModal.jsx`. Built and deployed to IIS.
- [x] Phase 149 (HR Administrator Broadcast Message Management & Dashboard Announcement Refactor) — Enabled HR Administrators and System Administrators to draft, send, edit, and remove organization-wide broadcast announcements via dedicated backend endpoints (`GET /broadcasts`, `POST /broadcast`, `PUT /broadcast/{id}`, `DELETE /broadcast/{id}`) and interactive `BroadcastModal.jsx`. Resolved defect where personal notifications (comments/likes) leaked into the Dashboard HR Broadcast banner. Added direct Edit & Remove controls to Dashboard and HR Analytics. Built and deployed to IIS.
- [x] Phase 150 (People Network Hero Badge — Connecting MPOnline Colleagues) — Aligned the People Network & Connections page with enterprise branding and page context by adding the `✨ Connecting MPOnline Colleagues` hero badge and ambient radial background gradient. Built and deployed to IIS.
- [x] Phase 151 (Remove False-Positive SSO Debug Console Logs) — Guarded SSO debug logging in `UserContext.jsx` to only fire when actual SSO tokens or employee credentials are present, eliminating misleading console logs during normal in-app navigation (such as `/community/view?id=192`). Built and deployed to IIS.




### Pending Modules

- None (All implementable FRD scope is 100% complete, patched, and hardened)

---

## Architecture

- Database First
- Repository Pattern
- Service Pattern
- ASP.NET Core 9
- SQL Server
- JWT Authentication
- React Frontend

---

## Database Notes

Database changes must originate from SQL Server.

Never manually modify scaffolded models.

---

## Important Decisions

- **Generic Event-Driven Notification Engine (Phase 11)**: `INotificationService.PublishAsync` / `PublishBroadcastAsync` are keyed by opaque `NotificationTypes` categories. Producers (Jobs is first) publish without modifying the notification core. Centralizes gating inside the engine.
- **Job auto-expiry** is handled by a `BackgroundService` (`JobExpiryHostedService`), not by mutating rows on read.

---

## Active Modules

- Audit Trail & Governance (Phase 10): `AuditLog` model + `IAuditLogRepository`/`IAuditLogService`; reusable `ISuspensionGuard`; `AuditLogController` gated to System Administrators; `UserService` suspend/activate actions record audit entries.
- Jobs & Notifications (Phase 11): `JobsController` gated to HR/System-Admin for CRUD with dynamic filtering and auto-expiry; generic `NotificationsController` handling preferences and center counts; Job posting publishes broadcasts via the engine.

---

## Pending Modules & External Integration Decisions

Per project external dependency decisions, the following features are intentionally **OUT OF SCOPE** for the current backend implementation because they depend on external systems under active development by other teams:

1. **HRMS SSO Integration (`/api/auth/sso-login`)**: Preserves existing JWT/role claim extension points and authentication architecture. Live external IDP / HRMS OIDC token exchange is pending external infrastructure delivery.
2. **Email Digest Engine (`FR-NT-02`)**: Background scheduler (`DigestHostedService`) and daily/weekly email digests depend on external corporate SMTP provisioning (`FRD INT-03`).
3. **HRMS-Triggered Synchronization Workflows**: Automatic daily employee department/role synchronization (`LastSyncedFromHrmsDate`) is deferred until external HRMS REST/OData endpoints become available.

Do **NOT** create dummy implementations, fake APIs, placeholder authentication flows, or simulated integrations for these items. They will be integrated only when live external enterprise endpoints are delivered.

---

## Known Issues

None

---

## Technical Debt

None

---

## Next Phase

**Frontend Handoff & Post-Launch Operations (`v1.2.8`)**: The backend (`Knome.API`) is permanently frozen (`v1.2.8`), fully production-ready, and verified (`0 warnings, 0 errors`, 100% DI PASS). All pre-frontend (`Phase A / B`), pre-deployment (`Phase C / D`), and documentation sync (`Phase E`) items have been resolved (`31 / 32 resolved`, 1 deferred post-launch infrastructure item `F-025`).

---

## AI Session Notes

Every new AI session should:

1. Read CLAUDE.md
2. Read PROJECT_CONTEXT.md
3. Read PROJECT_STATUS.md
4. Read latest Development Journal
5. Read relevant FRD section

### AI Resume Instructions

For any future AI session:

1. Read CLAUDE.md
2. Read PROJECT_CONTEXT.md
3. Read PROJECT_STATUS.md
4. Read only the latest Development Journal
5. Read only the relevant FRD section
6. Do not re-read older journals unless explicitly requested.
7. Keep reasoning concise and minimize unnecessary file reads.

Then summarize understanding before implementation.
